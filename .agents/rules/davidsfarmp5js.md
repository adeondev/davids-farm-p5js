---
trigger: always_on
---

1. Contexto Sempre em Primeiro Lugar: Antes de propor grandes mudanças ou novas arquiteturas, sempre considere as informações do README.md. Entenda a essência do jogo e as mecânicas planejadas antes de codificar.
2.Código Completo e Intacto: Quando eu pedir alterações, criações ou correções, você deve SEMPRE enviar o código do arquivo por completo, pronto para ser copiado e colado. NUNCA simplifique partes, não use abreviações (como // resto do código aqui) e NUNCA remova ou altere funções existentes sem que eu tenha pedido explicitamente.
3. Política Estrita de Comentários: NÃO adicione comentários ao código gerado a menos que eu peça de forma explícita. Qualquer código novo deve vir limpo. Mantenha os comentários que já existem no projeto intactos. Se for absolutamente necessário adicionar um comentário para explicar uma modificação complexa ou a leitura de um módulo, ele deve ser escrito APENAS em inglês.
4. Organização e Escalabilidade: Respeite a estrutura de pastas existente (src/, css/, arquivos na raiz). Se você notar que o código está ficando complexo, você tem total liberdade para sugerir e criar novas pastas e arquivos (ex: separar a lógica em diretórios de entities/, scenes/, core/), mantendo o projeto extremamente organizado.
5. Modularidade e Limpeza: Mantenha o src/sketch.js (ou o arquivo principal do p5.js) o mais limpo possível. Lógicas pesadas, classes, sistemas de colisão ou gerenciamento de cenas devem ser isolados em arquivos próprios, deixando o arquivo principal apenas para inicialização (setup) e chamadas de métodos (draw).
6. Integração Perfeita: Todo código gerado deve rodar perfeitamente com o ecossistema atual. Certifique-se de que as classes e funções conversem corretamente com o index.html e com as bibliotecas locais carregadas, como p5.js e p5.sound.min.js. Não adicione dependências de terceiros ou frameworks extras sem me consultar antes.
7. Idioma Estrito (Tudo em Inglês): Absolutamente TODO o código gerado deve ser escrito em inglês. Isso inclui nomes de variáveis, funções, classes, parâmetros, constantes e nomes de arquivos. O código deve seguir o padrão universal.